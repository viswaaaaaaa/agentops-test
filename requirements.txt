# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.0
pydantic>=2.7.4,<3

# LangChain + providers
langchain==0.2.16
langchain-google-genai==1.0.10   # Gemini (free Gemini Pro)
langchain-anthropic==0.1.23      # Claude (optional)
langchain-groq==0.1.9            # Groq fast routing (free)
langgraph==0.2.28                # multi-agent orchestration

# GitHub
PyGithub==2.4.0

# Database
# SQLite is built-in — no extra package needed for local dev
# For Supabase/Postgres: pip install psycopg2-binary sqlalchemy
